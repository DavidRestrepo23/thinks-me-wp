# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repo is the WordPress theme for "Think SME" (business incorporation/advisory site in Singapore). Tailwind build, design tokens (`src/base.css`), theme scaffold and the homepage sections all exist; ACF field groups are created and committed under `acf-json/`. Homepage order lives in `front-page.php`: hero, logos (certifications), Three Ways card deck, logos (clients), Start/Run/Grow accordion, Google Reviews slider, FAQ, final CTA.

**There is no gallery section** — `template-parts/gallery.php`, its `gallery_image` CPT and `assets/js/gallery-slider.js` were removed. `header.php` carries both the document shell (doctype, `<head>`/`wp_head()`, opening `<body>` that `footer.php` closes) and the site header, rebuilt from the newer Figma file; it can never be deleted outright, since `get_header()` is what emits that shell.

Newer sections (`hero`, `cards-deck`, `testimonials`, `faq`, `cta`, `footer.php`) were built against a **newer Figma file** ("Untitled", `vzdpOnH1U36oXcFcugiyE5`) than the one `docs/superpowers/specs/2026-07-30-*` describes, so treat that spec as historical — most of what it specifies (the old hero/gallery/header/CTA) no longer exists in this form.

Besides the homepage there are fourteen inner pages, `page-contact-us.php`, `page-registered-office-address.php`, `page-company-incorporation-local.php`, `page-company-incorporation-foreign.php`, `page-corporate-secretary.php`, `page-accounting-bookkeeping.php`, `page-corporate-tax.php`, `page-gst-registration.php`, `page-property-cashout.php`, `page-business-loan.php`, `page-remittance.php`, `page-mortgage-loans.php`, `page-psg-grant.php` and `page-mra-grant.php`. All follow the same pattern: a `Template Name:` header *and* a filename matching the page slug, with an ACF group located on `page_template == <that file>` — so WP picks the template either way, but **the template has to be selected in Page Attributes for the fields to appear** (ACF has no page-slug location rule; this is the mechanism, not a workaround). Each group repeats the Home group's `cta_*` **field names** under its own key prefix, which is what lets `cta.php` stay page-agnostic.

`page-contact-us.php` ("Contact us"), in Figma order `contact-form` (62:42) → `offices-map` (62:126) → the page's editor content if any → `hiring` (62:100) → the shared `cta`. Its group is `group_thinksme_contact` ("Contact Page Content"), keys prefixed `field_thinksme_contact_*`.

Contact page specifics:

- **The enquiry form is the theme's own**, not a plugin. Markup in `template-parts/contact-form.php`, everything else in `functions.php`: `thinksme_contact_fields()` is the single field list, `thinksme_handle_contact_submission()` runs on `template_redirect` (early enough to redirect after a successful send, so a refresh can't re-post), and `thinksme_contact_state()` carries errors/values/sent to the template. Delivery is `wp_mail()` to the Customizer's "Contact form recipient" (Site Details), falling back to `admin_email` — on a real host that needs an SMTP/API sender or it lands in spam. Spam guard is nonce + off-screen honeypot; no CAPTCHA.
- **Fields are pills whose label doubles as the placeholder** (`.contact-field` in `src/base.css`): a real `<label>` overlays the input and fades on focus/input, and the input's `placeholder` is a single space so `:placeholder-shown` stays a reliable "is empty" test. Figma has no filled state, so this is the reading of it that keeps labels accessible and the red asterisk its own colour.
- **`offices-map.php` is a live Google Maps embed** on the keyless `maps?q=…&output=embed` endpoint — no API key, no Cloud project. The four office cards are real links to Google Maps that `assets/js/contact-map.js` intercepts to re-point the iframe and move `data-active` (the selected palette follows the selection, like the positional card styling elsewhere in the theme); modifier/middle-click still open Maps, and with the script gone the section degrades to four links. Addresses come from flat `office_1..4_*` ACF fields — ACF free has no Repeater — and an office with an empty name *and* address is skipped. Below `lg` the cards stop overlapping the map and stack under it with their own border.

`page-registered-office-address.php` ("Registered Office Address"), in Figma order `roa-hero` (67:49) → the shared `logos-slider` (`certifications` group, same slot it has on the homepage) → `roa-plan` (67:109) → `roa-block` (67:211) → `roa-why` (68:564) → the shared `testimonials` → the page's editor content if any → the shared `faq` → the shared `cta`. Its group is `group_thinksme_roa`, keys prefixed `field_thinksme_roa_*`.

Registered Office Address page specifics:

- **The hero photo is two layers**, as in Figma: the landscape shot inside a rounded card, plus a transparent cut-out of the same building on top, which is the only reason the tower can break above the card's top edge. Both are ACF image fields; the cut-out is optional. The Figma crop offsets (`top-[-47.93%]`, `h-[152.9%]`, …) are measured against the *exported* photo and are meaningless for another image, so uploading a custom photo switches the card to a plain `object-cover` — see `$is_stock` in `roa-hero.php`.
- **`roa-plan.php`'s benefits are six flat `roa_plan_benefit_N` fields**, empties skipped (ACF free has no Repeater — same reason as `office_1..4_*`). The columns are `flex-col-reverse` below `lg`: Figma reads price-card-first left-to-right, but stacked the section heading has to come before the thing it introduces.
- **`roa-block.php`'s service cards expand on click.** All four ship identical markup; `assets/js/expand-cards.js` (was `roa-block.js` until the Business Loan page drew the same interaction) only moves `data-active` and `aria-expanded`, and every pixel of the open/closed geometry is `.roa-card*` in `src/base.css` — so with the script gone the card the template marked active stays open and the section still reads as the design. Widths animate as pure `flex-grow` (`3.15 ≈ 632/200.67`, open over collapsed); below `lg` the row becomes a stack and only the palette, title size and description change. **Every card is a `<button>`, even before the client writes its description** — gating on copy would ship a section with nothing to click, since Figma only writes copy for card 2. A card with copy is a disclosure and carries `aria-expanded`/`aria-controls`; one without is only a selection and carries neither, which is also what `expand-cards.js` keys off. The card that starts open is the first one that has copy — card 2, so the initial state matches the design. Card icons come from the ACF `roa_card_N_icon` selects, whose choices are injected at runtime by `thinksme_roa_icon_choices()` from `thinksme_roa_icons()`, exactly like the megamenu icons: add an SVG to `assets/images/icons/roa/` plus one line in that function, nothing to re-sync.
- **`roa-why.php` pins itself and scrubs a progress rail** — the rail is shared with `ci-requirements.php` on the Accounting & Bookkeeping page, so its markup, CSS and script are page-agnostic. `assets/js/progress-rail.js` finds any section carrying `data-progress-rail`, gives it a scroll runway (`100svh` + 0.5svh per connector) and writes one custom property, `--rail-progress` (0..1); `src/base.css` turns that into each connector's yellow fill via `clamp(0%, calc((var(--rail-progress) * var(--rail-lines) - var(--rail-step)) * 100%), 100%)`, where `--rail-lines` is set on the section by PHP and `--rail-step` per connector. **The pin is `position: sticky` — nothing hijacks the scroll** (no `preventDefault`, no `scrollTo`, no wheel handler), so progress hits 1 on the last step and the page simply carries on. `--rail-progress` defaults to 1, so the un-enhanced section (no JS, below `lg`, reduced motion, or a viewport too short to hold the rail — the script measures) is a plain block with every connector already full. Two gotchas the code is shaped around: the section's vertical padding is zeroed while pinned, because sticky travel is measured against the *content* box and leaving it in would stop the last connector short; and `.has-scroll-reveal #main-content > section[data-progress-rail]` drops the reveal `translateY`, since a transform on the sticky element's ancestor chain breaks `position: sticky`.
- **The Google Reviews slider and the FAQ are `testimonials.php` / `faq.php` verbatim** — same `testimonial` / `faq_item` CPTs, same Swiper loop/autoplay, same native `<details>` accordion. Only their hat and heading differ, and those come from `testimonials_*` / `faq_*`, which this page's ACF group repeats (heading defaults "Read Our Reviews" and the homepage's FAQ heading) exactly as it repeats `cta_*`. Swiper's CSS/JS is enqueued for this template too, along with `logos-slider.js` and `testimonials-slider.js`, all of which were previously homepage-only; the FAQ needs no JS.
- **The skyline behind the block heading is one exported SVG** (`assets/images/roa/skyline.svg`), not the ~150 separate vectors it is on the canvas. It carries its own navy rounded panel, so it sits over the section background seamlessly; the opaque `#1E1E1E` canvas rect Figma includes in that export was stripped by hand.

`page-company-incorporation-local.php` ("Company Incorporation Local", page ID 338), in the order the client asked for: `ci-hero` → the shared `logos-slider` (`certifications` group, same slot as the other two pages) → `ci-pricing` (85:1351) → `ci-tools` (85:1665) → `ci-includes` (85:1697) → `ci-ways` (85:1757) → `ci-why` (85:1830) → the shared `testimonials` → the page's editor content if any → the shared `faq` → the shared `cta`. Its group is `group_thinksme_ci`, keys prefixed `field_thinksme_ci_*`. **The five designed sections are loose frames on the Figma canvas, not an assembled page** — Figma defines no sequence here, so the order is the client's and not something to "correct" against the file.

Company Incorporation Local page specifics:

- **The free-tools tabs are the only interactive part, and the tools themselves do nothing yet.** Figma draws *one* panel (`85:1679`), not three: the tab strip is three labels, and only "Company Name Check" has copy written for it — the same situation `roa-block.php` is in. So all three tabs ship the identical panel shape and tabs 2–3 default to lorem, by explicit client decision. No ACRA name validation, no SSIC dataset, no tax computation. The panel's input+button is a plain GET form pointing at `ci_tools_tab_N_button_link` so the controls aren't dead chrome; nothing reads the `q` parameter. `assets/js/tabs.js` (was `ci-tools.js` — see the Property Cashout notes) only moves `data-active`/`aria-selected` and toggles `[hidden]`, with roving tabindex and arrow/Home/End keys; without it the first panel stays open. **Below `lg` the strip is a native `<select>` instead** (Figma `100:10` — a yellow pill with the active tool's name and a caret in a bordered circle), because three labels this long can't be a row on a phone. Both controls are always in the DOM and CSS shows exactly one; `display: none` keeps the hidden one out of the accessibility tree, so nothing is announced twice. The select's option values are the tab buttons' ids, so it drives the same `select()` the strip does and is written back to whenever the strip is what moved.
- **`ci-pricing.php`'s middle card is highlighted by position, not by a field.** `data-featured` is set on the second slot, and `.ci-plan*` in `src/base.css` keys the blue fill, the white type, the "MOST POPULAR" tab and the solid button off it — the same positional-styling convention `cards-deck.php` and `testimonials.php` use. A per-card "featured" checkbox would let the client tick all three and flatten the section. The three cards stretch to equal height so the buttons line up regardless of how many features each carries. **Below `lg` the row is a slider that loops and autoplays, opening on the highlighted card.** Three layers, each degrading to the one under it: the markup is a plain flex row, `src/base.css` turns it into a scroll-snap rail below `lg`, and `assets/js/ci-plans.js` swaps that rail for Swiper (loop, 5s autoplay, `initialSlide` = the featured slot). Swiper's `swiper`/`swiper-wrapper`/`swiper-slide` classes are stamped on by that script rather than written into the template, because they carry layout of their own — on desktop `.swiper`'s `overflow: hidden` would cut off the "MOST POPULAR" tab hanging above the middle card. Hence also the `.ci-plans__track` element (Swiper's required container/wrapper pair) and the `:not(.swiper)` gate on the rail's rules.
- **The cityscape behind the pricing heading is one exported SVG** (`assets/images/ci/pricing-bg.svg`), not the 149 separate vectors it is on the canvas — same treatment as `roa/skyline.svg`, including stripping by hand the opaque `#1E1E1E` canvas rect Figma puts in the export. `ci-why`'s navy card carries its own crop of the same artwork (`ci/why-card-bg.svg`), which needed no such strip.
- **`ci-why.php`'s five bento cards are five different shapes, written out rather than looped.** Card 1 is a wide photo with the copy over a scrim, 2 is plain white, 3 is a photo with no copy at all, 4 is navy, 5 is pale yellow with the photo above the copy. Which slot is which shape is fixed by the layout, so each card only carries the fields its shape uses — card 3 has no title field because card 3 *is* the photo. Figma serves those photos as 3300px PNGs (22 MB for one); they are committed downscaled to ~2× display size as JPEG. **Below `md` all five are pinned to the same 366px** — stacked in one column they read as a list, and Figma's mobile frame (`101:79`) letting each height follow its content (330..422) makes them look like five unrelated components. That height is card 2's, the tallest of the four copy cards; card 5 is the one whose content does not fit, so its photo carries the difference (`min-h-0` lets the flex item shrink, `object-cover` crops instead of squashing) and card 1's title/description gap drops to 16px. From `md` the grid takes over and every height is the row's again.
- **Headings on dark backgrounds need the colour repeated on the `h1`–`h6` itself.** `src/base.css` sets a hard `color` on the heading elements, and a rule on the element beats a colour inherited from a wrapper — so a `<div class="text-text-on-dark">` turns its `<p>` white but leaves the `<h3>` dark. This bit `ci-tools.php` and two `ci-why.php` cards during the build; the fix is an explicit `text-text-on-dark` on the heading.
- **`ci-hero.php` is node 91:2069.** Two buttons, not one — a solid split button plus an outlined one carrying the phone number, which is this page's own ACF field rather than the Customizer's site-wide number (it is a CTA label here, not site chrome; the two do not stay in sync). The brush stroke under the headline is desktop-only at a fixed offset, measured against the design's three lines at 72px — the same constraint `roa-hero.php` documents, so a much shorter headline will re-wrap and put the stroke under the wrong line. The lightbulb badge is Figma's nine separate vectors (91:2059–91:2067) merged into one SVG, the same treatment the pricing cityscape gets.
- **The hero's image wrapper is Figma's whole 609×554 group, not the 583×485 photo card.** The badge deliberately overhangs the card's top-left corner, so the group is bigger than the card; both children are placed as percentages of that box (card at 26,69; badge at 0,26). An earlier pass sized the wrapper to the card and used padding for the badge, which quietly ate 26px off the photo's width and left the badge floating beside the corner instead of over it. If a Figma group is larger than the thing it contains, that extra space is usually load-bearing.
- **`ci-includes.php` is node 85:1697, not 85:1703.** `85:1703` is only the card column; the section around it (`Group 2368`) also holds the centred heading `85:1698` and the photo column `85:1699`, so building from the inner node drops two thirds of the design. Worth remembering generally: a Figma node handed over as "the section" can be a child of it — check the parent before implementing, especially when a screenshot comes back much narrower than the page. Figma stacks two photo layers in `85:1699` (`85:1700` and `85:1702`); only the lower one is visible, the upper sits entirely behind it, and it is not carried over. The visible photo's crop offsets are Figma's placement of that specific export, so a client upload switches to a plain `object-cover` — the same `$is_stock` arrangement `roa-hero.php` uses. **Mobile is its own frame (`101:18`)**: the photo moves below the cards (`flex-col-reverse`, as in `roa-plan.php`) and each card stacks its icon above the copy instead of beside it, which also keeps a three-line title from stranding the icon mid-card. Icon size, card padding and title size do *not* shrink below `lg` — the mobile frame draws them at the desktop values.

`page-company-incorporation-foreign.php` ("Company Incorporation Foreign", page ID 350) is **the same page again**: Figma frame `101:846` is `page-company-incorporation-local.php` with different words and photographs, section for section, so it renders the same `template-parts/ci-*.php` rather than a second copy of them. The two diverge in exactly one place, `thinksme_ci_defaults()` in **`inc/ci-content.php`**, which keys off `thinksme_current_template()` and hands each part its own set of design fallbacks (copy, image filenames, and the one crop string `ci-includes.php` needs). Its group is `group_thinksme_cif`, keys prefixed `field_thinksme_cif_*` — including the section *tabs*, whose keys are `field_thinksme_tab_cif_*`: those don't carry the field-name prefix, so a straight find-and-replace over the Local group's JSON leaves nine colliding keys behind.

Company Incorporation Foreign specifics:

- **Duplicating the six template parts was the obvious move and the wrong one** — ~1,100 lines of identical markup, and every layout fix a two-file job. Leaving the copy in the database only was the other wrong one: it keeps the design out of git and lets a cleared field fall back to the *other* page's text. Hence the third option, defaults per template. If a third incorporation page ever lands, add a set to `thinksme_ci_content()`; don't add `cif2-*.php`.
- **The what's-included photo is two-layer on this page, one-layer on Local**, so the *shape* of that slot is a default too, not just the file: `image_box`, `image_class` and `image_upload_class` in `thinksme_ci_defaults('includes')`. Local's box is the 576x486 card, it clips, and the photo is cropped into it; Foreign's box is the whole 576x528 group (Figma "Frame 1171277395"), it does not clip, and the image is `object-contain` — the asset carries its own corners and the subject deliberately breaks above the card's top edge. Cropping it into the card ate exactly that overhang, which is what "no se ve completa" looked like. Foreign's *upload* branch is `object-contain` as well, because on this page the field's content is a composed export rather than a bare photograph.
- **The design's photos live in `assets/images/cif/`**, alongside `ci/`, same downscale-to-~2×-and-JPEG treatment. The hero is the exception: `ci-hero.php`'s stock branch wants one composed PNG (card + cut-out + badge on transparency), and Figma has no single node for it — the card (`101:857`), the cut-out that breaks above it (`101:861`) and the badge (`101:862`) are three siblings. It was composited by hand from the raw fills, because the node exports come back **flattened on white**: the cut-out's alpha only exists in the underlying image fill, not in what Figma renders for a PNG export. The canvas is padded to 659×600 so it matches the template's fixed `aspect-[609/554]` box.
- **The final CTA's photo differs, and `cta.php` is shared by every page**, so the fallback is exposed as the `thinksme_cta_photo` filter and `inc/ci-content.php` overrides it for this template only — rather than teaching a page-agnostic part which page it is on.
- **Figma frame `101:846` still says "Incorporation Packages for Local Founders" and "Why Local Founders Choose Think SME"**, and prices the Essential card as "solo local founders". Those are duplication artefacts left in the design file, not copy anyone wrote for this page; they ship verbatim because inventing replacement copy is the client's call, not the theme's. They are three strings in `thinksme_ci_content()`'s `$foreign` set and in the ACF group's `default_value`s.
- **Two icons were added for the bento** (`star`, `user-sound` in `assets/images/icons/ci/`, plus a line each in `thinksme_ci_icons()`), which is all it takes — the ACF select's choices are injected at runtime, so nothing needs re-syncing.
- **The `thinksme_cta_photo` filter is no longer Foreign-only.** It now returns whatever photo the current set names, which is the site-wide one for Local — so a third page can end on its own photograph without touching `cta.php`.

`page-corporate-secretary.php` ("Corporate Secretary", page ID 510) is **the same page a third time**: Figma frame `102:1969` is the incorporation page with different words and photographs, so it renders the same `template-parts/ci-*.php` and gets its own set in `thinksme_ci_content()` (`'secretary'`, written out in full rather than as a diff on Local — this page keeps almost none of the incorporation copy). Section order is the frame's own: `ci-hero` → `logos-slider` → `ci-pricing` → `ci-tools` → `ci-includes` → **`ci-why-slider`** → `ci-ways` → `ci-why` → `testimonials` → editor content → `faq` → `cta`. Its group is `group_thinksme_cs`, keys prefixed `field_thinksme_cs_*` and `field_thinksme_tab_cs_*` (the tab-key trap the Foreign group documents).

Corporate Secretary specifics:

- **`ci-why-slider.php` is the one section the other two pages don't have** (Figma `102:2254` + `102:2410` + `102:2260`): five cards, photo above a copy block, as a carousel — Figma draws the row 2703px wide inside a 1440px frame, so it is a slider on the canvas, not a grid. It is **not** a replacement for the `ci-why` bento, which is still on this page below it as "Why SMEs Switch to Think SME". The part reads `thinksme_ci_defaults('why_slider')` and returns early when a page's set has none, so it is inert in the other two templates. Three layers like `ci-plans`: plain flex row → scroll-snap rail in `base.css` → Swiper via `assets/js/ci-why-slider.js`, which stamps Swiper's classes on and reveals the prev/next pair the template ships `hidden`. Loops and autoplays like every other carousel in the theme (client's rule), just with a longer 6s delay since each card is a paragraph rather than a glanceable quote; pauses on hover, off entirely for reduced motion, stopped while out of view. **Slide `width` and `display` are repeated under `.ci-why-slider.swiper`** — `.swiper-slide` is a single class too and swiper-bundle.css loads after the theme's stylesheet, so the plain rule loses and every slide fills the viewport.
- **This page prices two packages and ships two free tools**, so the third pricing card and the third tool are present in the set with empty titles/labels and skipped by the loops that already skip empties. Its what's-included section carries no heading either, which is why `ci-includes.php` now drops the `<h2>` when the heading is empty rather than holding the section's gap open with an empty element.
- **The hero is the one set whose stock asset is the photo card alone.** The badge is vector art and Figma exports vectors flattened onto white — there is no rasteriser in this environment to bake the SVG into the PNG with its transparency intact — so the set names `ci/hero-badge.svg` in `badge` and `ci-hero.php`'s stock branch draws it over the card at Figma's offsets, the same file and placement the upload branch uses. The hero group's proportion (`image_box`), the photo's slot inside it (`photo_slot_class`), the badge placement (`badge_class`) and the brush stroke's offset (`underline_class`) are all per-page defaults now: the three frames draw the group at three different sizes, and the stroke is measured against each page's own headline.
- **The hero and what's-included photos were composited by hand** (PIL, from the raw image fills): both are two-layer in Figma — a rounded card plus a cut-out of the same photo that breaks above its top edge — and the node export comes back flattened on white. Unlike the Foreign page's hero, the cut-out layers here carry real alpha, so the composite is the two fills stacked at Figma's crop offsets, nothing painted by hand. Photos live in `assets/images/cs/`.
- **The lone centred button under the tool panel** (Figma `102:2253`) is `ci_tools_button_text` / `_link`, an optional CTA at the end of `ci-tools.php`. Figma leaves it floating between the tools and the what's-included frames; it is attached to the tools section because that is what it follows from — having just shown someone their deadlines, the section asks for the switch. The glyph inside the tool's input (`calendar-dots`, added to `thinksme_ci_icons()`) is a per-tab default rather than a client field: it names the format the field wants, which belongs to the tool.
- **Figma frame `102:1969` still says "Why Local Founders Choose Think SME"** over the carousel, and its intro still talks about setting up from overseas. Same call as the Foreign page's leftovers: they ship verbatim, because inventing replacement copy is the client's.
- **Page 510 was populated the same way page 350 was** — value + `_name` field-key meta row per field, images left empty so the theme files win — which is what makes the shared `cta.php` and `testimonials.php` show this page's copy ("Ready to switch?", "Read Our Reviews") rather than their generic defaults.

`page-accounting-bookkeeping.php` ("Accounting & Bookkeeping", page ID 512) is **the same page a fourth time**, and the first one whose Figma frame (`102:3127`) is an assembled page rather than loose section frames — so its section order is read straight off the canvas: `ci-hero` → `logos-slider` → **`ci-grid` "switching"** → `ci-pricing` → `ci-includes` → `ci-why` → **`ci-requirements`** → **`ci-grid` "same_firm"** → `testimonials` → editor content → `faq` → `cta`. Its set is `'accounting'`; its group is `group_thinksme_ab`, keys prefixed `field_thinksme_ab_*` and `field_thinksme_tab_ab_*`.

Accounting & Bookkeeping specifics:

- **It is the first page to *drop* sections rather than add them**: this frame has no free-tools tabs and no two-ways section, so the set simply defines no `tools` / `ways` and the template doesn't call those parts. That is the same mechanism `ci-why-slider.php` uses in reverse, and it is why `thinksme_ci_defaults()` returning `array()` has to stay a supported answer.
- **`ci-grid.php` is one part rendering two Figma frames** — "Switching to Think SME" (`102:3146`, centred, 3+2 cards, 112px icon discs) and "The Same Firm for Compliance, Digital, and Financing" (`102:3726`, left-aligned, 3×2 with a photograph in the fifth cell, 80px discs). It is called with `array( 'instance' => … )` and reads `thinksme_ci_defaults( 'grid' )[ $instance ]`. The grid is `lg:grid-cols-6` so both frames fit one layout: cards carry their own `lg:col-span-N lg:min-h-[…]` as **one literal class string in `inc/ci-content.php`**, because Tailwind only generates classes it can find in source — a class assembled from a number at runtime never exists. Heights are `lg:`-prefixed on purpose: they exist to line rows up, and stacked on a phone they would just be dead space.
- **The grid's photo cell is Figma's two-layer arrangement again**, and it is bottom-aligned and absolutely positioned *from `lg` only*. Below `lg` the cell has no row to break out of, and an absolute image in a zero-height cell lies across the card above it — so there it stays in the flow at its natural height.
- **`ci-requirements.php` reuses ROA's progress rail, which is why that rail is now shared code.** `roa-progress.js` became `assets/js/progress-rail.js`, driven by `data-progress-rail` / `data-progress-pin` instead of the `.roa-why` selector, and the CSS moved from `.roa-why__*` to `.rail*` with `--rail-progress` / `--rail-lines` / `--rail-step`. Mechanism unchanged and still `position: sticky` with **nothing hijacking the scroll** — no `preventDefault`, no `scrollTo`, no wheel handler. "The scroll stays until the steps complete" is a sticky pin plus a runway, so a visitor who wants past it just keeps scrolling.
- **This section's rail had to be sized against the viewport, not Figma.** ROA puts its copy beside the rail; this frame stacks the heading above it in one navy panel, which at Figma's 126px connectors is ~1100px tall — taller than a laptop viewport, and the script refuses to pin a block that doesn't fit (it would park the bottom off screen). So `--rail-connector` is `clamp(56px, 7svh, 126px)` here, the panel is `py-xl` rather than `py-3xl`, and the rail is `mt-xl`. Those two spacing choices are **unconditional, not keyed on `[data-pinned]`** — the script measures the block *unpinned* to decide whether it fits, so a rule that only applies once pinned can never help it get there.
- **The rail needs `relative` to paint over the cityscape.** The artwork is an absolutely positioned `<img>` and the `<ol>` was in normal flow, so the SVG's 470px band covered the first two steps — invisible icons and titles with the connectors showing through. Positioned elements paint above in-flow content regardless of DOM order; `ci-pricing.php` already carries the same `relative` for the same reason.
- **`ci-why.php`'s card 3 now takes copy when a set gives it some.** This frame writes a title and paragraph into that slot ("Foreign-Owned, Singapore-Registered"), so with a title the card takes card 1's shape — scrim, icon disc, copy over the photo — and without one it stays the wordless picture the other three pages draw. No flag: the presence of a title decides it. `ci-includes.php` grew the same way, with an optional hat/intro/button above it for this frame's `102:4133` header.
- **The hero strikes two lines, not one.** Figma draws `102:3131` and `102:3132` under a four-line headline, so the hero sets can now name an `underline_2_class`; the other three leave it empty and draw one stroke as before.
- **The hero and what's-included photos ship as flattened exports rather than hand composites.** Both are two-layer in Figma, and Figma exports them flattened onto white — which is exactly this page's ground (`body` is `#ffffff`), so the flattened PNG is pixel-identical to the composite and there is nothing to reassemble. That shortcut is only valid because the section behind them is white; the CS and CIF pages needed the PIL composite for their own reasons. Photos live in `assets/images/ab/`.
- **Thirteen icons were added to `assets/images/icons/ci/`** plus a line each in `thinksme_ci_icons()`, and `functions.php` now registers the icon-select filter for `ci_why_card_3_icon`, the two grids and the requirements steps.
- **This is the first of the two pages whose sections carry extra air between them** (Property Cashout is the other): `<main>` has `sections-spaced`, and `.sections-spaced > section + section` in `src/base.css` adds 80px from `lg` (40px below it) on top of the padding the sections already have. A class on this page's `<main>` rather than margins inside the parts (they are shared with four other pages) and rather than a `page-template-*` body class (that only exists when the template was picked in Page Attributes — the trap `thinksme_current_template()` documents). The hero is excluded by `section + section`, and margins sit outside `offsetHeight`, so the pinned rail measures and scrubs unchanged. **The rail itself takes more than that** — `#main-content > section.ci-requirements` is 120px from `lg` (56px below), on both pages that draw it: it is a full-height pinning panel, so it reads as a chapter break rather than the next block in the flow. That selector carries an id because `.sections-spaced > section + section` (0,2,0) outranks any `mt-*` utility on the section, so a class in the template would have moved Corporate Tax only. It applies to Property Cashout's rail too, which is a third page now.

`page-corporate-tax.php` ("Corporate TAX", page ID 514) is **the same page a fifth time**, and the second assembled frame (`108:4497`), so its order is read off the canvas: `ci-hero` → `logos-slider` → **`ci-stats`** → **`ci-grid` "needs"** → `ci-pricing` (split layout) → **`ci-calculator`** → **`ci-penalties`** → `ci-why-slider` → `ci-requirements` → `testimonials` → editor content → `faq` → `cta`. Its set is `'tax'`; its group is `group_thinksme_ct`, keys prefixed `field_thinksme_ct_*` and `field_thinksme_tab_ct_*`. It drops four sections the others have — no free-tools tabs, no two-ways, no what's-included, no bento.

Corporate Tax specifics:

- **The five-step rail only pins once it fits the viewport, which took ~236px off the panel.** `progress-rail.js` refuses to pin a block taller than the screen — it would park the bottom off screen — and five steps at the Accounting page's sizes come out ~968px against a ~732px viewport, so the section shipped static with every connector already full. The savings are a group of rules under `.ci-requirements:has(.rail__step:nth-child(5))` in `src/base.css`: a 32px connector floor (`clamp(32px, 4.4svh, 126px)`), a heading and step titles that scale with the viewport (`clamp(40px, 5.5svh, 64px)` / `clamp(20px, 2.8svh, 28px)`), and the panel padding, rail margin and per-step gap one step down the scale. On a tall display every clamp lands back on Figma's own numbers, so this is a short-viewport accommodation rather than a second design. **Keyed on the fifth step existing, not on a class or a data attribute** — the count is what makes the panel too tall and `:has` can read it, so the four-step rail is untouched and PHP doesn't have to describe its own layout. The rules are unconditional rather than `[data-pinned]`-scoped for the reason the Accounting notes give: the script measures the block *unpinned* to decide whether it fits.
- **Worth knowing: the Accounting rail has the same ceiling and hasn't been given the same treatment.** Its four-step panel measures ~803px, so it stops pinning below roughly an 805px viewport — a 1440×900 window with a toolbar is already borderline. The Corporate Tax one now pins from ~730px.
- **"The Same Firm for Compliance, Digital, and Financing" is the pinned rail here, not `ci-grid`.** That heading is the Accounting page's second grid instance, and the obvious move — reuse `ci-grid` because the heading matches — is wrong: this frame (`108:4664`) draws five icon rows joined by connectors, which is `ci-requirements`. Read the drawing, not the heading. Its rail is five steps rather than four, so the icon-select filter loop in `functions.php` now runs 1..5.
- **`ci-pricing.php` draws two layouts now**, chosen by a `layout` default per page: `'panel'` (the navy panel with the cards on it, every other page, unchanged) and `'split'` (this frame's `108:4833` — section copy plus a four-check list on the left, one package card on the right, on white). The card itself was moved out to **`template-parts/ci-plan.php`** so both branches render the same markup; what the variant changes is where "featured" shows (`data-featured="true"` is the panel's blue fill and tab above, `"badge"` is the split's faint card with the label as a pill inside), the check-badge size (24px vs Figma's 40px here), the card padding, whether the feature list has rules between items or one above it, and whether the button sits inside the card. `data-variant` carries it into CSS. A section pricing one package also gets `.ci-plans--single`, which switches the mobile rail's bleed and peek off — they exist to show the next card and there isn't one.
- **`ci-calculator.php` is the one tool in the theme that actually computes.** `ci-tools.php` deliberately does nothing (Figma wrote no behaviour for its three inputs); this frame draws a calculator, names the 17% rate and names both exemption schemes, so a Calculate button that only navigated away would break the promise on the label. The rate and each scheme's bands are **defaults in `inc/ci-content.php`** (`[band, exempt %]` applied to the bottom of chargeable income: SUTE 75%/100k then 50%/100k, PTE 75%/10k then 50%/190k) and `assets/js/ci-calculator.js` reads them off the markup — they are IRAS's numbers and they move with the Budget. The schemes are **native radios** styled with `:has(:checked)`, so the choice needs no script and travels with the form; with JS off the whole thing is a GET form to the contact page. Figma draws no result block, so that one is built from the panel's own vocabulary and rounded to whole dollars.
- **`ci-grid.php` grew a `grid_class` default** (falling back to `lg:grid-cols-6`): this frame's four equal cards can't divide six columns, so the `needs` instance names `lg:grid-cols-4`. Cards 3 and 4 of that grid repeat the same title and copy — a duplication artefact in the design file, shipped verbatim like the Foreign frame's leftovers.
- **`ci-plans.js` duplicates its cards too, below four, and removes the copies on teardown.** Same Swiper loop floor, but this slider only exists below `lg` — above it the section is the design's plain row, where a duplicate card is an extra column nobody drew. So the copies are made in `build()` and removed in `teardown()`, and nothing in that file holds on to the original NodeList any more. Fixing this also surfaced a real crash: the IntersectionObserver that pauses autoplay off-screen was never disconnected, so the first callback after a teardown threw `Cannot read properties of null (reading 'autoplay')` — it is now disconnected and the callback guards on the instance.
- **`ci-why-slider.js` now duplicates its slides when there are fewer than six.** Swiper needs roughly twice the visible count before it will loop, and at `slidesPerView: 'auto'` neither this page's three cards nor Corporate Secretary's five clear that — Swiper was disabling the loop and saying so in the console on both pages. The copies are `aria-hidden`. Loop + autoplay is the client's rule for every carousel, so a silently linear slider was not an option.
- **Three icons were added** (`percent`, `clock-countdown`, `calculator`) plus a line each in `thinksme_ci_icons()`, and the icon-select filter now also covers `ci_grid_needs_card_N_icon` and `ci_stats_card_N_icon`. Photos live in `assets/images/ct/`; the hero and the CTA are flattened Figma exports (this frame's ground is white, so nothing is lost), and slide 3's export came back clipped at the canvas edge — the row is 1609px wide inside 1280 — so it was rebuilt from the raw fill at Figma's own crop.
- **Six tokens were added** to `src/base.css` and mirrored in `src/tailwind.config.js`: `brand-yellow-tint`/`brand-yellow-strong` (the selected scheme), `surface-navy-deep` (the VS badge and the numbered discs, deeper than every other navy panel), `surface-success-soft`/`border-success`/`text-success` (the "filed on time" side of the comparison).

`page-gst-registration.php` ("GST Registration", page ID 516) is **the same page a sixth time**, and the third assembled frame (`114:5287`), so its order is read off the canvas: `ci-hero` → `logos-slider` → **`ci-grid` "needs"** → `ci-pricing` (panel) → **`ci-penalties`** → `ci-includes` → `ci-why-slider` → `ci-why` → `testimonials` → editor content → `faq` → `cta`. Its set is `'gst'`; its group is `group_thinksme_gst`, keys prefixed `field_thinksme_gst_*` and `field_thinksme_tab_gst_*`. It drops four sections the others have — no free-tools tabs, no two-ways, no figures band, no pinned rail.

GST Registration specifics:

- **It is the first page to add nothing but *values*.** Every frame on it is one an earlier page already draws, so no new template part was written; what it did need were three optional bits inside two existing sections, each rendered only where a set names it: the hero's promotional price line (`ci_hero_price_text` / `_price_strike` / `_price_note`, Figma `114:5296`), the registration card's struck-through usual price with its terms (`ci_pricing_card_N_price_strike` / `_price_note`, `114:5518`), and the small print closing the pricing panel (`ci_pricing_note`, `114:5502`). Two tokens came with them, `text-navy` (#10243c) and `text-faint` (#aeb6c2, the struck figures).
- **Two sections are the trap this family keeps setting.** "The Cost of Missing a Filing" is `ci-penalties` — the Corporate Tax comparison — with `steps` left empty, so the escalation stages under it are skipped rather than invented. And "The Same Firm for Compliance, Digital, and Financing" is the bento here (`114:5834`), not the pinned rail Corporate Tax draws under the same heading: `ci-why`, not `ci-requirements`. Read the drawing, not the heading. Card 3 carries copy over its photograph, as on the Accounting page.
- **The hero sets its headline a step smaller than every other frame** (64px, `114:5294`), so `title_class` is a per-page hero default now with the other five frames' 72px as the fallback. It strikes two lines, like the Accounting one.
- **Figma writes "U.P S$400 strikeoff" into the hero's second price label.** The word is an instruction to the designer, not copy: it ships as the line-through in `ci-hero.php` and the label reads "U.P S$400". Two other strings *are* shipped verbatim as design artefacts, the same call the Foreign frame's leftovers got: the fourth what's-included card stops mid-sentence on a semicolon ("Small errors can be corrected in your next F5;"), and the final CTA still talks about computation and filing "from S$850 per Year of Assessment", which is the Corporate Tax page's offer.
- **The highlighted pricing card draws both of its pills filled** (`114:5578`) where every other frame outlines the first one, so `badge_1_style` is a per-card default ('outline' unless a set says 'soft').
- **Photos live in `assets/images/gst/`** and ship as flattened JPEG exports: the hero and what's-included groups are two-layer in Figma, Figma flattens them onto white, and white is this page's ground — the same shortcut the Accounting and Corporate Tax pages take. The bento's wide first card was rebuilt from the raw fill (Figma exposes that cell only as the whole composed card, text and all) and centre-cropped to the card's 849x398.
- **Three icons were added** (`hand-heart`, `gauge`, `file-arrow-up`) plus a line each in `thinksme_ci_icons()`; the grid instance is called `needs`, so it reuses the icon-select filters the Corporate Tax page already registers.
- **Page 516 was populated the same way pages 350, 510 and 514 were** — value + `_name` field-key meta row per field, images left empty so the theme files win — which is what makes the shared `cta.php`, `faq.php` and `testimonials.php` show this page's copy.

`page-property-cashout.php` ("Property Cashout", page ID 537) is **the same page a seventh time**, and the first one built from a **different Figma file** — "Think SME- INTERNAL" (`CgqSxvxd3aQeQSkLPhc48q`), frame `951:8993`, not the "Untitled" file the other six came from. It is the fourth assembled frame, so its order is read off the canvas: `ci-hero` → `logos-slider` (**`lenders`**, not `certifications`) → **`ci-definition`** → **`ci-compare`** → `ci-requirements` → **`ci-types`** → **`ci-stack` "benefits"** → **`ci-eligibility`** → **`ci-stack` "why"** → editor content → `faq` → `cta`. Its set is `'cashout'`; its group is `group_thinksme_pc`, keys prefixed `field_thinksme_pc_*` and `field_thinksme_tab_pc_*`. It drops seven sections the others have — no pricing, no free-tools tabs, no two-ways, no what's-included, no figures band, no calculator and no bento — **and no Google Reviews slider**, because this frame draws none.

Property Cashout specifics:

- **It is the first set that is mostly new sections rather than new values.** Five parts were written for it and are inert everywhere else (their `thinksme_ci_defaults()` key is absent, so they return early, the same mechanism `ci-why-slider.php` uses in reverse): `ci-definition.php`, the pale panel with the worked example; `ci-compare.php`, the eight-row table against standard refinancing; `ci-types.php`, the four LTV columns; `ci-stack.php`, the overlapping-card frame; and `ci-eligibility.php`, the navy two-tab checklist. Only four sections are shared — the hero, the logo marquee, the pinned rail and the final CTA.
- **`ci-stack.php` is one part rendering two Figma frames**, called with `array( 'instance' => 'benefits' | 'why' )` like `ci-grid.php`. "Key Benefits" (`951:9557`) puts the photograph left, "Why Choose Think SME" (`951:9444`…`951:9627`) puts it right; everything else is identical. The cards overlap by 48px (Figma's 187px pitch against a 235px card) and **the last one is navy and tilted `-2.07deg` by position, not by a field** — and that card is also the row's hover state, tilt and all: any card takes the fill and the rotation while hovered (lifting over the card it overlaps) and the resting dark one straightens up and hands both over, so exactly one card is ever dark and tilted and it follows the pointer. The tilt's 4px shift is part of the `transform` rather than a margin, or switching it on and off would reflow the column — the same convention `cards-deck.php`, `testimonials.php` and `ci-pricing.php` follow, and for the same reason: a per-card checkbox would let the client tick all five and flatten the section. Stacking order is a z-index per slot (`--ci-stack-index`, set by PHP) because each card has to paint over the one it overlaps. Below `lg` both the overlap and the tilt go away — pulled together in one column they read as a broken list, and a rotated full-width card overflows the viewport.
- **`ci-compare.php`'s row label is one field, not two.** Figma prints the same eight labels down both columns, so storing them twice would be eight extra fields whose only job is to stay identical. It is a CSS grid rather than a `<table>`: each `.ci-compare__row` is `display: contents` from `lg`, so its two cells become items of the table grid and line up across rows, and below `lg` it is a bordered card whose cells each carry a tag naming their column — a header eight rows up is no use on a phone. The check/warning badges are the section's own art, not fields: they say which side of the comparison a cell is on.
- **`ci-definition.php` is the one place a ci-* field keeps inline markup.** The design bolds runs *inside* sentences (the lead-in, the three figures in the worked example, the word "Formula:"), so three fields render through `wpautop()` + `wp_kses()` with a short allow-list instead of `esc_html()`. **`p` has to be in that allow-list** even though no field contains one — `wpautop()` is what adds the paragraphs and kses strips any tag the list doesn't name, which is exactly how the intro's blank line silently produced one run-on paragraph the first time. That double-duty is also what lets the intro be Figma's two paragraphs from a single field.
- **The hero grew three things, all optional and all empty on the other six frames**: `ci_hero_text_2` (this is the only frame whose hero runs to two paragraphs), the three figure pills over the photograph, and per-page column widths. **The widths are load-bearing, not cosmetic**: this frame draws 619px of copy beside a 583px image group where the others draw 703/609, and the copy column is what breaks the headline into three lines — at 703px it rewraps and the first pill lands on top of it. `body_class` / `image_col_class` are per-set defaults for the same reason `underline_class` already was.
- **The pills are a sibling of the photo, not a child of it.** Inside the aspect-ratio box they were in normal flow under an absolutely positioned `<img>`, and a positioned element paints above in-flow content regardless of DOM order — so they simply didn't appear. The column and the photo's own box are now two elements: the column carries `relative` for the pills to position against above `lg` (their wrapper is `lg:contents`, so each pill resolves against the column rather than the wrapper), and the inner box keeps the aspect ratio the `badge_class` / `photo_slot_class` percentages are measured from, which is what leaves the other six sets untouched. Below `lg` the pills stack under the photo instead of being hidden — they carry real figures, so hiding them would drop content rather than simplify a layout. Their icons are whole 56px discs rather than glyphs, because Figma draws the navy disc and the yellow mark as one vector group.
- **`ci-hero.php`'s upload branch now gates the badge on `badge_class`, not just on the upload.** This is the first set whose frame draws no badge at all, and an unclassed `<img>` landed in the flow at its natural size.
- **The pinned rail needed a third round of short-viewport savings, and the last one is counter-intuitive.** This frame's process rail (`951:9220`) adds an intro under the heading and a timing label under each step ("DAY 1", "WITHIN 24 HRS", …) on top of the five steps the Corporate Tax one already has, which put the panel at ~1008px — back above a laptop viewport, exactly the case `progress-rail.js` refuses to pin. The savings are a block under `.ci-requirements:has(.rail__step:nth-child(5)):has(.rail__meta)`. **Both `:has()` are chained on purpose**: `:has()` takes its argument's specificity, so `:has(.rail__step:nth-child(5))` is (0,2,0) against `:has(.rail__meta)`'s (0,1,0) and the five-step block would win every shared value no matter what order they sit in — the first attempt silently did nothing for that reason. The cheapest saving of all is the last one: **on a short viewport the rail gets *wider*** (`max-width: clamp(588px, calc(1400px - 90svh), 860px)`, a clamp that runs backwards), so each description wraps to one line instead of two. Figma draws the rail 588px inside a 1392px panel, so width was never the constraint — height was, and two lines of copy cost far more than 130px of extra width. It now pins from a ~677px viewport, better than the Corporate Tax rail's ~730px, and every clamp lands back on Figma's numbers on a tall display.
- **This is the first page whose FAQ questions are fields rather than the site-wide `faq_item` CPT.** The frame writes six of its own, about property cashout and nothing else. `faq.php` now prefers a set's `faq` list when there is one (`faq_item_N_question` / `_answer`, 1..8, empties skipped) and falls back to the CPT otherwise — `thinksme_ci_defaults()` returns `array()` outside the ci-* templates, so the homepage, the contact page and the other six are byte for byte what they were. Deliberately not a `faq_group` taxonomy on the CPT: that would put this page's questions in the same admin list as the site-wide ones and make "which page does this appear on" something the client has to get right on every future FAQ.
- **`ci-tools.js` became `assets/js/tabs.js`** — the same generalisation `roa-progress.js` → `progress-rail.js` got, and for the same reason: `ci-eligibility.php` draws a second tab strip whose panel looks nothing like the free-tools one. It is driven by `data-tabs` / `data-tab` / `data-tab-panel` / `data-tab-select` instead of one section's class names, and it initialises **every** group on the page rather than the first one found. The `.ci-tools__*` classes stay exactly what they were — styling hooks. There is no `<select>` fallback on the eligibility strip: two labels this length still fit as a row on a phone, where three didn't.
- **Figma draws only the first eligibility tab's panel**, the same situation `ci-tools.php` is in. Tab 2 ("Documents Typically Required") ships with the label and title the design writes and **its seven items empty** — unlike the free-tools tabs, which default to lorem, because a list of documents a client will be held to is their copy to write. Its photograph carries `lg:col-start-2` explicitly, or with no checklist it becomes the grid's first item and slides to the left.
- **The scattered-blocks artwork behind the two navy panels** is `assets/images/pc/panel-bg.svg`, this frame's replacement for the cityscape the other rails draw — hence `background` and `rail_class` becoming per-set defaults in `ci-requirements.php`. Exported as one SVG from the vectorised node and hand-stripped of the `#BFBFBF` canvas rect, the page-sized white rect and the opaque navy panel rect, the same treatment `roa/skyline.svg` and `ci/pricing-bg.svg` got.
- **Photos live in `assets/images/pc/`** and ship as flattened exports. The hero, the eligibility card and the CTA are two-layer in Figma and flatten onto white, which is what actually sits behind them — the eligibility one included, because its layers sit inside the *white* panel rather than on the navy one. The definition photo is the exception worth knowing: Figma flattens onto whatever is really behind the node, and behind that one is the `#fbfbfb` panel, so **that JPEG is exact against `--color-surface-panel` and would show a seam on any other ground**. The four property-type photos were cropped out of the row export; the lender logos were cropped out of a 4× export of the marquee and trimmed to a 96px cap.
- **Seven icons were added** to `assets/images/icons/ci/` (`house`, `magnifying-glass`, `trend-down`, `stack`, `lock-key-open`, `user-focus`, `briefcase`) plus a line each in `thinksme_ci_icons()`, and the icon-select filter now also covers `ci_stack_benefits_card_N_icon` / `ci_stack_why_card_N_icon`. The page's decorative art that is never a client choice — the three pill discs, the info glyph, the warning glyph and the circled mark over the comparison heading — lives in its own `assets/images/icons/pc/`.
- **Four tokens were added** to `src/base.css` and mirrored in `src/tailwind.config.js`: `surface-navy-rich` (#0b2558 — the comparison table's own column header and the hero's figure pills, between `surface-dark` and `surface-navy-deep`), `surface-slate` (#515f7b — the "Standard Refinancing" header beside it), `surface-yellow-pale` (#f8f7e5 — the worked-example callout) and `border-pale` (#f3f3f3 — the comparison table's rules).
- **The logo marquee shows lenders, not certifications.** This frame's seven logos are the banks and financiers the offer depends on, so they went into a new `logo_group` term `lenders` (term 9) as seven `client_logo` posts (539–551, attachments 540–552) and the template passes `array( 'group' => 'lenders' )`. `logos-slider.php` renders nothing when a group is empty, so that slot needs the posts to exist at all.
- **Page 537 was populated the same way pages 350, 510, 514 and 516 were** — value + `_name` field-key meta row per field (147 of them), images left empty so the theme files win — which is what makes the shared `cta.php` and `faq.php` show this page's copy.
- **`docs`/spec note:** the two frames this page reuses under a *different* heading are the trap this family keeps setting, and it did not fire here — but the `ci-requirements` call is still the one to read carefully. "How Does Property Cashout Work in Singapore?" is the pinned rail because the frame draws five icon rows joined by connectors, not because of anything in the heading.


`page-business-loan.php` ("Business Loan", page ID 553) is **the same page an eighth time**, and the fifth assembled frame — back in the "Untitled" file (`119:1711`) after Property Cashout took its frame from the newer one, so its order is read off the canvas: `ci-hero` → **`ci-stats`** → **`ci-steps`** → **`ci-calculator`** → **`ci-grid` "approval"** → **`ci-ways`** → **`ci-stack` "bankers"** → **`ci-stack` "why"** → **`ci-types`** → `testimonials` → editor content → `faq` → `cta`. Its set is `'loan'`; its group is `group_thinksme_bl`, keys prefixed `field_thinksme_bl_*` and `field_thinksme_tab_bl_*`. It drops six sections the others have — no pricing table, no free-tools tabs, no what's-included cards, no bento, no card carousel and no pinned rail — **and no logo marquee**: this frame's logos are five partner banks inside the hero, not the full-width `logos-slider` strip.

Business Loan specifics:

- **One new part, `ci-steps.php`** (`119:1796`): "Get Funded in 3 Simple Steps", three cards where the open one widens, steps its title up and shows a description. That is `roa-block.php`'s interaction on a different palette — pale cards on white with a navy open one, where ROA's are navy on a navy panel with a yellow open one — so **`roa-block.js` became `assets/js/expand-cards.js`**, driven by `data-expand-cards` / `data-expand-card` and initialising *every* group on the page. Same generalisation `ci-tools.js` → `tabs.js` and `roa-progress.js` → `progress-rail.js` got, and the `.roa-card*` / `.ci-step*` classes stay what they were, styling hooks in `src/base.css`. Which card is open is **the first one with copy** — Figma writes a description for the middle card only, and the cursor it draws over that card is the designer's own, not chrome.
- **Everything else this frame adds is a value, not markup.** `ci-stats` grew an optional footnote (`119:1795` — the headline figure is a *flat* rate and the small print is what says so); `ci-grid` grew an intro line and a `lg:grid-cols-3` track; `ci-types` grew a three-check list per card, its own track and a per-instance `image_box` (landscape here, portrait on Property Cashout); `ci-hero` grew the partner-bank strip. `ci-ways` and `ci-stack` are untouched.
- **`ci-calculator.php` draws two modes now**, chosen by a `mode` default per page the way `ci-pricing.php`'s `layout` is: `'tax'` (Corporate Tax — two exemption schemes, one income field) and `'loan'` (this frame's `119:1825` — an amount, a native tenure `<select>` and a flat rate). The panel, its yellow tab, the heading block, the result block and the disclaimer are shared; only the field set and the sum differ. **The sum is the flat-rate method the panel's own disclaimer names** — `total = amount + amount × rate × years`, instalment = total / months — deliberately not an amortising schedule, because computing a reducing balance under a label that says "flat rate" answers a different question than the one on screen. `assets/js/ci-calculator.js` reads `data-mode` rather than sniffing which fields exist. The tenure list (1..15 years, spanning the EFS working-capital ceiling of 5 and the fixed-asset one of 15) is the control's own vocabulary, like the calculator's rounding: Figma draws only the closed select showing "3 Years".
- **"What Actually Determines Loan Approval" is `ci-grid`, and the trap this family keeps setting nearly fired again.** Five icon cards with a photograph in the fifth cell is the Accounting page's "Same Firm" grid, not a new section. Its photo cell needed one thing the other instances didn't: **the photograph is cropped to the cell's own proportion** rather than left taller than it. `ci-grid` draws that cell's image from the cell's bottom edge so a composed export can overhang the row above, which is right when the overhang is 22px into a gap — but this frame's cards carry copy down to their bottom padding, the row gap is 15px, and the export overhung 55px straight across the card above. The cell also carries `overflow-hidden rounded-lg` in its class string, because this export has no rounded corners baked in.
- **The partner-bank strip is a marquee beside its label, not a static row.** Figma draws `119:1737` as a masked carousel — 505px of logos inside a 439px window with a rectangle fading each end — so it runs on `assets/js/logos-slider.js`, the same script the logo sections use, and the fade is a `mask-image` on `.ci-hero__banks` rather than two gradient overlays (the strip sits on the page background, and an overlay would have to know its colour). Two things came back to that shared script as attributes: `data-slides-desktop="auto"`, which sizes each slide to its own logo instead of to an equal share of the track, and `data-space-desktop`, since 64px of homepage gap reads as four logos adrift inside a 642px column. The five logos are **rendered twice**, `aria-hidden` on the copies, because Swiper wants roughly twice the visible count before it will loop — the floor `ci-why-slider.js` clones around. The Standard Chartered wordmark is clipped mid-letter on the canvas by that mask, so only its mark is carried over. They are theme files under `assets/images/bl/banks/` rather than the `client_logo` CPT: they are the evidence for the hero's "19 core partner banks", the same reason the Property Cashout pills' discs are defaults.
- **Six icons were added** to `assets/images/icons/ci/` (`chart-bar`, `question`, `seal-warning`, `arrows-split`, `binoculars`, `scales`) plus a line each in `thinksme_ci_icons()`, and the icon-select filter now also covers `ci_steps_card_N_icon`, `ci_grid_approval_card_N_icon` and `ci_stack_bankers_card_N_icon`. `assets/images/icons/caret-down.svg` is new too — the tenure select's caret, handed to CSS as `--ci-calc-caret` the way `faq.php` hands over its +/- icons.
- **Photos live in `assets/images/bl/`** and ship as flattened exports; this frame's ground is white, so nothing is lost. The hero is the whole 642x586 image group (the lightbulb badge overhangs the card's top-left corner, so the group is bigger than the card — the constraint `ci-hero.php` documents), and the two `ci-stack` photographs were cropped to the photo itself, since that part clips them into its own rounded box.
- **This page's FAQ questions are fields, like Property Cashout's.** The frame writes six of its own about business lending (`119:2144`) and an answer for the first only; the rest open to just the question until the client writes them, which is what `faq.php`'s set-driven branch already does.
- **Page 553 was populated the same way pages 350, 510, 514, 516 and 537 were** — value + `_name` field-key meta row per field (147 of them), images left empty so the theme files win — which is what makes the shared `testimonials.php`, `faq.php` and `cta.php` show this page's copy ("See Why Founders Recommend Think SME", "Questions SMEs Ask Us First", "Ready to explore financing?") rather than their generic defaults. The group's JSON was generated from the `'loan'` set rather than hand-written, so its `default_value`s and the theme's design fallbacks cannot drift apart.


`page-remittance.php` ("Remittance", page ID 555) is **the same page a ninth time**, and the shortest frame in the family (`123:2687`) — four sections and the footer, read off the canvas: `ci-hero` → **`ci-grid` "why_ofx"** → **`ci-signup`** → editor content → `faq` → `cta`. Its set is `'remittance'`; its group is `group_thinksme_rm`, keys prefixed `field_thinksme_rm_*` and `field_thinksme_tab_rm_*`. It drops nine sections the others have — no pricing table, no free-tools tabs, no figures band, no calculator, no what's-included, no bento, no card carousel, no pinned rail and no logo marquee — **and no Google Reviews slider**.

Remittance specifics:

- **It is a partner page, not a service page**, and that shows up in two places. The hero opens on **OFX's logo where every other frame draws a hat pill** (`123:2692`) — a pill reading "OFX" above a headline reading "Save with OFX" says it twice — so `ci-hero.php` grew a `logo` / `logo_class` / `logo_alt` slot that renders *instead of* the hat, not as well as. It is a per-page default rather than a field: it is the mark of the company the page is about, not copy the client writes. And the copy names the partner throughout, which is why the FAQ and the CTA below are the site-wide ones — this page sells someone else's rates, not a Think SME package with its own questions.
- **The hero's brush stroke is its own vector.** Every other frame strikes a whole line with `assets/images/icons/ci/hero-underline.svg`; this one strikes the single word "OFX" mid-line (`123:2690`), and scaling that long stroke down to a word's width thins it to a pencil line. So `underline_file` is a per-set default now (`rm/hero-underline.svg`, exported with its 7.74deg baked in), with the shared stroke as the fallback every other set still takes.
- **`ci-grid.php` can sit inside a panel now.** "Why Use OFX?" (`123:2715`) is the Accounting page's centred grid — three cards, 112px icon discs, 24px titles — drawn on a pale rounded block with **white** cards instead of on white with `#f8f8f8` ones. Two class strings per instance carry that (`panel_class`, `card_class`), so it stays one part and one layout rather than a second grid.
- **`ci-signup.php` is the one new part** (`123:2747` + `123:2748`): a centred headline, two buttons, and a photograph tilted into each margin. Deliberately **not `cta.php` under another name** — this frame draws both, the sign-up band here and the shared closing CTA below it, so folding them together would cost one of the two its shape. **The two photographs are decoration, so they are theme files and `aria-hidden` rather than image fields**: they carry nothing the headline doesn't, and a client upload would land at a tilt measured for a specific export (Figma rotates them on the canvas, so the rotation is baked into the exports). They appear **from 1280px only** — between `lg` and there the centred column wants the same pixels, and a 300px photo per side would lie across the headline, so the band keeps its copy and drops them.
- **The asterisk in "FX Transfers*." is the design's own**: it points at the fee note in the first card above it. Shipped verbatim, the same call the Foreign and GST frames' leftovers got.
- **No new icons.** The three cards use `star`, `shield-check` and `user-sound`, all already in `thinksme_ci_icons()`; only the icon-select filter learned the new grid instance (`ci_grid_why_ofx_card_N_icon`). Photos live in `assets/images/rm/` as flattened exports (this frame's ground is white), and the OFX mark is `rm/ofx-logo.svg` — Figma's node export came back wrapped in the whole page with an opaque `#1E1E1E` canvas rect, so the vector-only asset from the same call is what shipped, the same trap the Standard Chartered mark set on the Business Loan page.
- **Its FAQ takes an extra step of air, and that is a CSS rule rather than a change to the shared part.** The sign-up band's left photograph hangs below the band's own box, so the 80px `.sections-spaced` gives adjacent sections left the FAQ's hat 53px under a photograph where Figma leaves ~100px of clear page. `faq.php` is shared by every page, so the extra margin is scoped to a body class — **`tpl-remittance`, added by `thinksme_template_body_class()`** from `thinksme_current_template()` rather than WP's own `page-template-*` class, which exists only when the template was picked in Page Attributes (the trap that function documents). The rule carries `#faq` to outrank `.sections-spaced > section + section`, the same reason `#main-content > section.ci-requirements` carries an id. That body class is now available to every template; it is for spacing that belongs to one page inside a shared part, not a licence to style pages from CSS — a difference that belongs to a section still belongs in `inc/ci-content.php` as a value.
- **This page needs no per-page JavaScript at all** — no tabs, no rail, no calculator, no carousel — so it is deliberately *not* added to the ci-* enqueue block in `functions.php`. The FAQ accordion is native `<details>`, as everywhere.
- **Page 555 was populated the same way pages 350, 510, 514, 516, 537 and 553 were** — value + `_name` field-key meta row per field (29 of them), images left empty so the theme files win. The group's JSON was generated from the `'remittance'` set rather than hand-written, as the Business Loan one was.


`page-mortgage-loans.php` ("Mortgage Loans", page ID 557) is **the same page a tenth time**, frame `124:3265`, and the first one that **adds no new template part at all**: `ci-hero` → `logos-slider` (`certifications`, with this frame's caption under it) → **`ci-stats`** → **`ci-grid` "types"** → **`ci-ways`** → **`ci-grid` "why"** → **`ci-grid` "decision"** → `testimonials` → editor content → `faq` → `cta`. Its set is `'mortgage'`; its group is `group_thinksme_ml`, keys prefixed `field_thinksme_ml_*` and `field_thinksme_tab_ml_*`. It drops six sections the others have — no pricing table, no free-tools tabs, no calculator, no what's-included, no bento and no pinned rail.

Mortgage Loans specifics:

- **`ci-grid` is called three times on one page**, which is what that part exists for: `types` is the four centred cards, `why` the navy panel, `decision` the fixed-or-floating pair. Read the drawing, not the heading — all three are the same component with different values.
- **Six optional bits went into sections that already existed**, each rendered only where a set names it. On the hero: `ci_hero_promo_text` (the wide pill between the intro and the buttons, `124:3274`) and `underline_2_file`, because this frame's two strokes are *different lengths* — a short one under "Best", a long one under "Mortgage Rates" — and scaling one vector to the other's width changes its weight. On `logos-slider`: the caption under the marquee, read from this page's set the way `faq.php` reads its own question list, so the homepage's two calls are byte for byte what they were. On `ci-ways`: a fourth check (the features loop now runs over whatever the set names rather than a fixed range), `ci_ways_plan_note` for the line about which packages qualify, and a per-set `heading_class` — Figma sets this heading in a 602px box where the incorporation frames use 512px, and that is what breaks it across two lines instead of four. On `ci-grid`: `heading_class` / `hat_class` for the instance on the navy panel, an optional `value` between a card's title and its copy (the two rates), and an optional `note` under the cards.
- **A navy panel needs the colour on the heading itself.** `base.css` sets a hard `color` on `h1`–`h6`, so a colour inherited from the panel wrapper loses — the same thing that bit `ci-tools.php` and two `ci-why.php` cards. Hence `heading_class` rather than a class on the wrapper. The hat is a second case of the same thing: Figma draws a **white** pill on navy where every other instance draws the pale-yellow one, so its fill is a value too.
- **The headline breaks across four lines, and Figma's 701px text box is a red herring.** That box is wider than the text needs; the frame renders "Compare Best / Mortgage Rates / Across 19 Banks / in One Call" inside its 656px column, and both stroke offsets are measured against those four breaks. An earlier pass widened the column to 701px to force three lines and put the strokes on the wrong words — check the rendered frame, not the text node's width.
- **Two strings ship as design artefacts, and one doesn't.** The section heading "Switching to Think SME, Without the Hassle" is the Accounting page's wording over this frame's own four cards, and the rates disclaimer appears twice (under the figures band and again under the rate cards); both ship as drawn — the second one is where the rates it qualifies actually are. But the hero button's "Copare My Rate" ships **corrected**: the same frame spells the word in its own headline, so that is a typo rather than copy, unlike the Foreign and GST frames' leftovers.
- **Seven icons were added** to `assets/images/icons/ci/` (`house-line`, `arrows-clockwise`, `chart-line`, `lock-key`, `trend-up`, `user-circle`, `bell-ringing`) plus a line each in `thinksme_ci_icons()`, and the icon-select filter now also covers `ci_grid_types_card_N_icon`, `ci_grid_why_card_N_icon` and `ci_grid_decision_card_N_icon`. The hero's two brush strokes live in `assets/images/icons/ml/`; its photograph is `assets/images/ml/hero-image.jpg`, the whole 594x519 image group flattened onto white.
- **Its FAQ and final CTA are the site-wide ones**, as on the Remittance page: this frame draws the homepage's own questions and its closing photograph — verified against `assets/images/cta/cta-photo.jpg` rather than assumed — so the set names neither a `faq` list nor a `cta` image.
- **Page 557 was populated the way every page since 350 has been** — value + `_name` field-key meta row per field (90 of them), images left empty so the theme files win. The group's JSON was generated from the `'mortgage'` set, as the Business Loan and Remittance ones were.

`page-psg-grant.php` ("PSG Grant", page ID 559) is **the same page an eleventh time**, frame `127:512` in the "Untitled" file, and the first one whose credentials strip replaces the logo marquee: `ci-hero` → **`ci-accreditations`** → **`ci-grid` "benefits"** → **`ci-grid` "features"** → **`ci-criteria` "criteria"** → `ci-requirements` → **`ci-criteria` "invoicenow"** → **`ci-grid` "credentials"** → `testimonials` → editor content → `faq` → `cta`. Its set is `'psg'`; its group is `group_thinksme_psg`, keys prefixed `field_thinksme_psg_*` and `field_thinksme_tab_psg_*`. It drops eight sections the others have — no pricing table, no free-tools tabs, no figures band, no calculator, no what's-included cards, no bento, no card carousel and no pinned two-routes block.

PSG Grant specifics:

- **Two new parts, and both exist because the frame draws them twice or not at all elsewhere.** `ci-criteria.php` is the green-ticked list beside a photograph, called with `array( 'instance' => 'criteria' | 'invoicenow' )` the way `ci-stack.php` and `ci-grid.php` are: "Am I Eligible, and How Do I Apply?" (`127:580` + the photo group `127:618`) puts the photograph right and titles its list, "InvoiceNow" (`127:901` + `127:622`) mirrors it and closes on three partner marks instead. DOM order is always copy-then-photograph and `photo` only moves the picture with `lg:order-*`, so below `lg` both read heading → copy → list → photo with no mobile spec. `ci-accreditations.php` is the two marks between rules under the hero (`127:895`) — deliberately **not** `logos-slider.php`: that section is a CPT-driven marquee because the list is variable and client-owned, where these two are the credentials the whole page rests on, so they are per-set defaults and theme files and a client logo can never land between them.
- **`ci-grid.php` is called three times and grew seven optional class strings**, every one defaulting to what the instances before it drew: `heading_size_class` (the benefits band sets 48px, between the shared 40/64 steps), `header_class` (that heading is measured in a 552px box), `card_pad_class` (its four columns have no card behind them, so no padding either), `disc_class` / `icon_class` (56/32 against the centred 112/48), `title_class` / `text_class` (28px white) and `card_justify_class`. That last one is the only one that fixes a *bug* rather than carrying a value: the centred instances distribute a card's height with `justify-center`, which is right while every card's copy runs to the same number of lines and wrong the moment one title wraps further — the discs stop lining up across the row, which is exactly what "Long-Term Sustainability" and "IMDA Pre-Approved PSG Grant Vendor" do. All three PSG instances name `justify-start`; nothing else changed.
- **The hero headline is a paragraph, not a display line** (40px, `127:519`), which is the one case the per-page `title_class` couldn't express: the shared ramp already sets 40px on a phone, so naming only the desktop step would leave the tablet band *larger* than the desktop one. Hence `title_size_class`, which replaces the whole ramp. The copy column is 620px rather than 703 for the reason the Property Cashout set gives — Figma sets the headline in that box and both brush strokes are measured against the lines it produces.
- **The hero's third Figma layer draws nothing and is not carried over.** The image group is a badge (`127:527`), a rounded photo card (`127:524`) and a cut-out of the same shot behind it (`127:526`) whose own top 57px are empty — so the frame renders card + badge and the cut-out is invisible. Worth checking before compositing: the first pass stacked all three, and the cut-out's white-backed export covered the card. The shipped export is the card composited under the badge onto white (PIL, 2×), verified against a 1:1 render of the whole frame at a mean per-channel difference of 4.5. The same full-frame render is what the other three photographs were checked against (1.2, 0.9 and 4.2) — cropping the frame and diffing it against the export is a cheap way to catch a layer you got wrong.
- **`badge_file` is new on `ci-hero.php` and is not `badge`.** The stock export already has the badge composed in, so the stock branch draws none; `badge_file` is what the *upload* branch paints back over a plain photo, which until now was hardcoded to the incorporation badge.
- **"How to Apply" is the pinned rail, and this frame writes the whole step into the title.** Four icon rows joined by connectors (`127:627`) is `ci-requirements` — read the drawing, not the heading, as ever. Unlike the other three rails it writes no second line and no timing label, so `text` and `meta` stay empty and the part renders neither; at four steps it pins comfortably and needs none of the short-viewport savings the five-step ones carry. Its scattered-blocks artwork **is** `assets/images/pc/panel-bg.svg`, the Property Cashout export, verified identical rather than assumed — a fresh export of this node came back the same drawing wrapped in an extra `#1E1E1E` canvas rect and a page-sized white one.
- **The shared CTA and reviews slider grew filters, like the CTA photo already had.** This frame closes on "Let's work together" (`127:865`) and titles its reviews "See Why Founders Recommend Think SME" (`127:859`), so `cta.php` and `testimonials.php` expose their fallback strings as `thinksme_cta_title` / `thinksme_cta_text` / `thinksme_testimonials_hat` / `thinksme_testimonials_heading` and `inc/ci-content.php` answers them from the current set — rather than either part learning which page it is on. `faq.php` needed no filter for the same job: it already reads a set's own `faq` list, so its `hat` and `heading` live beside the questions there.
- **Eight icons were added** to `assets/images/icons/ci/` (`lightbulb`, `plant`, `folder-open`, `archive`, `chart-pie-slice`, `megaphone`, `compass`, `folder-simple-star`) plus a line each in `thinksme_ci_icons()`, and the icon-select filter now also covers `ci_grid_benefits_card_N_icon` / `ci_grid_features_card_N_icon` / `ci_grid_credentials_card_N_icon` — **1..8 rather than 1..6**, since the features band is the widest grid in the family. Where the frame's Phosphor glyph already had a lucide equivalent in the set (`shield-check`, `users-three`, `trend-down`, `gauge`, `file-text`, `buildings`, `user-check`, `star`) the existing icon is reused rather than a second copy shipped.
- **Photos and marks live in `assets/images/psg/`**, the two brush strokes in `assets/images/icons/psg/`. The strokes are this frame's own vectors, not the shared `ci/hero-underline.svg` scaled: same brush, different length-to-weight ratio, so scaling one to the other's width changes how heavy it reads — the call the Remittance and Mortgage sets already made.
- **Page 559 already existed** (published, slug `psg-grant`, no template chosen) with the generic `group_thinksme_page_cta` meta on it — `cta_title` = "Need help?", `cta_button_link` = "#" — which **would have won over the design defaults**, since `thinksme_field()` only falls back when the stored value is empty. It was populated the way every page since 350 has been (value + `_name` field-key meta row per field, 134 of them, images left empty so the theme files win) and `_wp_page_template` was set so the ACF group appears in Page Attributes. The group's JSON was generated from the `'psg'` set, as the Business Loan, Remittance and Mortgage ones were. The two placeholder "PSG Xero Grant" menu items (megamenu item 19, footer item 530) were pointed from `#` at `/psg-grant/`.

`page-mra-grant.php` ("MRA Grant", page ID 562) is **the same page a twelfth time**, frame `130:1489` in the "Untitled" file, and the second grant page: `ci-hero` → **`ci-statement`** → **`ci-criteria` "covers"** → `ci-requirements` → `ci-accreditations` → **`ci-grid` "solutions"** → `testimonials` → **`ci-criteria` "expect"** → editor content → `cta`. Its set is `'mra'`; its group is `group_thinksme_mra`, keys prefixed `field_thinksme_mra_*` and `field_thinksme_tab_mra_*`. It drops ten sections the others have — no pricing table, no free-tools tabs, no two routes, no what's-included cards, no figures band, no calculator, no bento, no card carousel, no logo marquee — **and no FAQ**, which is the difference worth knowing against the PSG page: this frame writes no questions at all, so the set names no `faq` list and the template makes no `faq` call. Adding the site-wide FAQ would be adding a section the design doesn't have.

MRA Grant specifics:

- **One new part, `ci-statement.php`** (`130:1512` + `130:1513` + `130:1514`): a centred 40px sentence with a hand-drawn ring around "70%" and one button under it. Deliberately **not** `ci-definition.php` — the other "what is this thing" block in the family is a pale panel with a heading, a worked example and a photograph in a two-column grid, and this frame draws none of those, so folding them together would cost five optional class strings and a branch that skips four of five blocks. Deliberately not `cta.php` either, for the reason `ci-signup.php` gives: this frame draws that section too, at the bottom. The ring is the section's own art rather than a field (it circles the one figure the sentence turns on), placed as a share of Figma's own 1039px text box and **desktop-only**, like every brush stroke in the theme — it is measured against this sentence breaking into five lines at 40px.
- **`ci-criteria.php` grew exactly one optional block, and everything else that differs was already a value.** Both instances here close on a button (`130:1541`, `130:1827`) where the two PSG ones close on the list or on a row of partner marks — hence `button_text` / `button_link`. Copy, photograph, which side it takes, whether there is an intro or a sub-heading: all of that was already per-instance, which is why one part now renders four frames.
- **This hero is the only one in the family with no brush stroke**, and that needed `underline_class` set to `hidden` rather than left empty: `ci-hero.php` always emits the `<img>`, so an unclassed one lands in the flow at its natural size. Its headline is 64px (the GST frame's step, so only the desktop step is named, not the whole ramp) and breaks across four lines in Figma's 656px column.
- **The photographs were cropped out of a 2× render of the whole frame rather than exported per node**, because each is a stack Figma has no single node for — the hero is a badge, a rounded photo card and a cut-out of the same shot that breaks above the card's top edge, and the two criteria photos are the same two-layer arrangement. A per-node export of the hero came back as the cut-out alone on transparency, which is what a wrong-looking photo means here. The crop is auto-trimmed to the non-white bounding box, which is also what fixes each `image_box` aspect (618/529, 547/606, 547/561) — the group's own box carries dead space the drawing doesn't. The CTA photo is the raw image fill from the CTA instance, since `cta.php` crops it itself.
- **"Who can apply For MRA Grant in Singapore?" is the pinned rail** (`130:1694` + `130:1698`) — four icon rows joined by connectors, read the drawing not the heading, as ever. Like the PSG rail it writes the whole criterion into the *title* and no timing label, so `text` and `meta` stay empty; its scattered-blocks artwork is `assets/images/pc/panel-bg.svg` again, and its rail is 852px wide rather than the 588/718 the other rails take.
- **"Our solutions" is `ci-grid` on the pale panel with white cards**, the same shape the Remittance and PSG frames draw — four cards, 20px titles, no hat and no intro line. The icon-select filter loop for `ci_grid_solutions_card_N_icon` runs 1..4.
- **Two icons were added** (`handshake`, `storefront` in `assets/images/icons/ci/`, plus a line each in `thinksme_ci_icons()`); the other four glyphs the frame draws (`buildings`, `compass`, `chart-pie-slice`, `chart-bar`, `scales`, `megaphone`) were already in the set and are reused rather than shipped twice. Photos and marks live in `assets/images/mra/`, the statement ring in `assets/images/icons/mra/`.
- **Its reviews slider and closing CTA are the shared ones verbatim** — this frame draws the component defaults, and `cta.php`'s own "Need help?" and its line are already this frame's words — so the set names no `testimonials` block and its `cta` names only a photograph.
- **Page 562 already existed** (published, slug `mra-grant`, no template chosen) with the generic `group_thinksme_page_cta` meta on it, including `cta_button_link` = `#` — which **would have won over the design defaults**, the same trap page 559 set. It was populated the way every page since 350 has been (value + `_name` field-key meta row per field, 81 of them, images left empty so the theme files win) and `_wp_page_template` was set so the ACF group appears in Page Attributes. The group's JSON was generated from the `'mra'` set. The placeholder "MRA Grant" footer item (item 532) was pointed from `#` at the page.

- **`get_field()` never returned the ACF `default_value` here.** Several groups define the same field *names* (`cta_*`, `faq_*`, `testimonials_*`, and now every `ci_*`), so ACF's name lookup resolves to whichever group's field post it finds first, fails the strict check against this post's groups, and falls through to raw meta — `NULL` when there is none. That is why the design lives in PHP defaults and why the Local page renders from stored meta: it was saved once in wp-admin. Page 350 was populated the same way (value + `_name` field-key row per field, images left empty so the theme files win), which is what makes the shared `cta.php` show this page's heading rather than its own generic one.

**`thinksme_field()` returns its `$default` when the stored value is `''`** (`functions.php`), which means a field that ships with copy cannot be blanked from the editor — clearing it brings the design text back. That is the theme's design-fidelity-fallback convention and it predates this page (`roa-plan.php`, `roa-block.php` and `offices-map.php` all rely on it), but it is worth knowing before promising a client that emptying a field removes a card. Only slots whose default is already `''` (e.g. pricing features 4–5 on cards 2 and 3) actually disappear when left empty. The ACF instructions on this page say so explicitly.

After editing anything under `acf-json/`, the field group has to be synced once in wp-admin (ACF > Field Groups > Sync) before the client sees it.

The WP install runs via Local by WP Engine, site `thinksme-local`, rooted at `/Users/davidrestrepo/Desktop/studio/thinks-me/app/public` — **which is also the git repo root**, so theme files are tracked under the `wp-content/themes/thinksme/` prefix rather than at the top level.

There is no `wp` on `$PATH`, but Local bundles WP-CLI and its own PHP, so the database *is* reachable from the shell — useful for assigning a page template or checking what a page actually renders, instead of guessing:

```
PHP=~/Library/Application\ Support/Local/lightning-services/php-8.2.29+0/bin/darwin-arm64/bin/php
SOCK=~/Library/Application\ Support/Local/run/3B7PzzVVr/mysql/mysqld.sock
"$PHP" -d mysqli.default_socket="$SOCK" \
  /Applications/Local.app/Contents/Resources/extraResources/bin/wp-cli/wp-cli.phar \
  --path=/Users/davidrestrepo/Desktop/studio/thinks-me/app/public post list --post_type=page
```

The socket path contains the Local site id (`3B7PzzVVr`) and is read from that site's `conf/mysql/my.cnf`; without `-d mysqli.default_socket` WP-CLI fails with "Error establishing a database connection".

## Specs

Design/planning docs live in `docs/superpowers/specs/YYYY-MM-DD-<slug>.md`, one per phase of work. The current spec is `2026-07-30-homepage-theme-design.md`, scoped to **Homepage only** — other pages from the Figma file are deferred to later phases following the same pattern. Read the relevant spec before making architectural decisions; it defines scope boundaries (what's explicitly out of scope for the phase) as well as the content model.

## Planned architecture (per current spec)

The theme is custom-built starting from `_s` (Underscores) — no page builder, minimal bloat. Theme slug: `thinksme`.

**Styling**: Tailwind CSS compiled via npm (`npm run dev` for watch, `npm run build` for production). Tailwind theme config (colors/spacing/typography) is mapped from the Figma file's styles/variables, not invented ad hoc.

**File layout** (matches the spec; `index.php` is an unlisted addition — WP won't recognize a theme without it):
```
thinksme/
  style.css                theme header (WP requires this; carries no CSS rules — see functions.php note)
  functions.php             setup, menu locations, enqueue, CPT registration, ACF json save/load hooks, thinksme_field() helper
  index.php                 fallback template (required by WP; out of scope beyond that)
  front-page.php            homepage — loops template-parts in Figma order, inside The Loop (see below)
  header.php
  footer.php
  template-parts/           hero, logos-slider, cards-deck, cards-stack, testimonials, faq, cta,
                            contact-form, offices-map, hiring,
                            roa-hero, roa-plan, roa-block, roa-why,
                            ci-hero, ci-pricing, ci-plan, ci-tools, ci-includes, ci-ways, ci-why,
                            ci-why-slider (Corporate Secretary, Corporate Tax + GST Registration),
                            ci-grid (Accounting & Bookkeeping, Corporate Tax, Business Loan,
                            Remittance + Mortgage Loans),
                            ci-requirements (Accounting & Bookkeeping, Corporate Tax + Property Cashout),
                            ci-stats, ci-calculator (Corporate Tax + Business Loan),
                            ci-penalties (Corporate Tax only)
                            ci-definition, ci-compare, ci-stack, ci-eligibility
                            (Property Cashout only), ci-types (Property Cashout +
                            Business Loan), ci-steps (Business Loan only),
                            ci-signup (Remittance only),
                            ci-criteria (PSG Grant + MRA Grant), ci-accreditations
                            (PSG Grant + MRA Grant), ci-statement (MRA Grant only)
                            (the ci-* parts serve all twelve of those pages — see inc/ci-content.php)
  inc/                      class-thinksme-nav-walker.php (megamenu), ci-content.php (per-template
                            copy and images for those twelve pages)
  acf-json/                 ACF field group exports (empty until the field group is created in wp-admin)
  assets/
    css/                    Tailwind build output (gitignored-worthy but not yet ignored — ask before changing)
    js/
    images/
      icons/                small SVGs pulled from Figma via the MCP asset URLs (arrow-up-right, check, logo-google, star)
      fonts/                Charlevoix Pro .otf files (client-provided, not in Figma)
  src/
    tailwind.config.js       theme.extend mirrors the CSS custom properties in base.css — keep both in sync
    base.css                 @font-face + :root design tokens, imported at the top of input.css
    input.css                @import base.css, then @tailwind base/components/utilities
  package.json
```

**Content model** (ACF free tier — no Options Pages, since those are PRO-only):
- Fixed-quantity content per page (hero copy, the 3 overview cards, the 3 Three Ways cards, section hats/headings, CTA) → ACF field group assigned directly to the "Home" page.
- Variable-length lists the client edits like normal posts (add/edit/delete from the WP admin sidebar, no code or layout access) → Custom Post Types: `testimonial` (labelled "Google Reviews" in wp-admin), `faq_item`, `hero_image` ("Hero Photos"), `client_logo`.
- ACF field groups are exported to JSON (`acf-json/`) and committed to git, so they're reproducible across environments instead of relying on manual export/import.

**Assets**: images/icons/vectors are exported from Figma (via the Figma MCP `download_assets` tool), optimized, and placed in `assets/images/`. Purely decorative, static vector art (e.g. the constellation-style background in the Overview section) is exported as static SVG rather than recreated in code — evaluate case by case.

**Responsive**: the provided Figma design is desktop-only. Build mobile-first with Tailwind and use reasonable judgment for tablet/mobile breakpoints where the Figma has no explicit spec.

## Implementation conventions

- **`thinksme_field( $selector, $post_id = false, $default = '' )`** (in `functions.php`) wraps `get_field()` so templates don't fatal before ACF is installed/configured, and normalizes empty values to a default. Always use it instead of calling `get_field()` directly in templates.
- **`front-page.php` runs the whole section list inside the standard Loop** (`while ( have_posts() ) : the_post(); ...`). That's deliberate, not boilerplate: with no post ID, `get_field()`/`thinksme_field()` read whatever page is the current post — which is the static front page, exactly where the Home ACF field group is assigned. Don't move the `get_template_part()` calls outside the Loop.
- **Per-page script enqueues gate on `thinksme_current_template()`, never `is_page_template()`.** The latter reads the `_wp_page_template` post meta, so it is only true when the template was chosen in Page Attributes — but the theme's page templates are *also* named for their page slug, so WP serves them to pages that never had the attribute set, and every per-page script then silently fails to load on a page that is plainly using that template. `thinksme_current_template()` records the basename WP actually resolved, via the `template_include` filter (which runs before `get_header()` fires `wp_enqueue_scripts`). The ACF groups still need the template selected — ACF has no page-slug location rule — but the JS no longer does.
- **Nav menus, not hardcoded links.** Header nav and the 3 footer columns are `wp_nav_menu()` against registered locations (`primary`, `footer-1`, `footer-2`, `footer-3`), so the client manages them from Appearance > Menus rather than editing PHP. `wp_nav_menu()` only lets you class the `<ul>`, not each `<li>/<a>` — per-item styling (header item padding, the caret on parents, the megamenu panel, the mobile panel) lives as plain CSS in `src/base.css`. The mobile burger is `assets/js/header-nav.js`, which only flips `data-nav-open` on the header.
- **The header megamenu is `Thinksme_Nav_Walker`** (`inc/class-thinksme-nav-walker.php`, Figma node 63:294) — the theme's only Walker, used because the panel markup is something `<ul class="sub-menu">` can't express. **Menu depth is the layout**: a 2nd-level item *with* children becomes a column (its title is the heading, its children are the cards); a 2nd-level item *without* children becomes a single card in its own column cell, so a two-level menu renders a row of cards instead of breaking. Card icons come from the ACF `menu_icon` select on menu items (`group_thinksme_menu_item`, located on `nav_menu_item`), whose choices are injected at runtime by `thinksme_menu_icon_choices()` from `thinksme_menu_icons()` — add an SVG to `assets/images/icons/megamenu/` plus one line in that function and the field updates itself, with nothing to re-sync in `acf-json/`. **The Finance & Loan and Grants cards each have their own icon; the Corporate Service ones are deliberately left as they were** — three carry an icon of their own and the other seven fall back to `network`, which is the client's call, not a gap to fill. All of that is post meta on the menu items, so it lives in the database, not in git, and reaches production only through the DB export step below.
- **An icon copied out of `assets/images/icons/ci/` needs its stroke re-weighted.** Those are Phosphor exports at whatever box Figma drew them in (32, 40, 48…) carrying a stroke that renders ~1.125px once the card scales them to 24px, against the 1.5px of the Lucide set the megamenu started with and the design draws. The copies in `megamenu/` are normalised to `stroke-width = 1.5 × viewBox ÷ 24`; the originals in `ci/` are untouched, since they render at 32–48px there and are correct as they are. The card's second line is the menu item's **native Description field**, which the client has to switch on once under Screen Options.
- **There are two panels, and the walker picks between them.** A top-level item whose children are 2–5 items that have no children of their own opens the **dropdown** (Figma node 172:148): a 544px panel under its own nav item holding one column of the same cards, hairline between each pair. Anything else opens the full-width megamenu. `Thinksme_Nav_Walker::is_dropdown()` decides and marks the `<li>` `.menu-item--dropdown`; everything visual is `.menu-item--dropdown …` in `src/base.css`. **Both halves of that rule matter** — "Corporate Service" is three 2nd-level items, so a count alone would turn the design's own megamenu into a 544px dropdown; what makes a panel a dropdown is that it is a plain list of cards. Six or more cards go back to the wide grid, where they wrap into rows instead of running past the fold. The dropdown is centred on its nav item, so an item near either edge of the nav would push it off screen; no menu currently has one there. - **Card hover/focus is one state for both panels** (Figma "Frame 1171277531"): a navy pill behind the row with the type and caret white, the yellow icon tile untouched. It is painted by an inset `::before` rather than by padding on the card — padding would grow the row and reflow the panel under the pointer — so the fill bleeds into whatever gap the panel leaves between cards. Two things had to give way for it: the megamenu's **invented** hover (a caret nudge, a navy title, a lighter icon tile, written when the design had no hover state) is gone, since its title colour tied on specificity and won on source order, painting navy type on the navy pill; and `.megamenu__card`'s `padding: 0` had to be restated as `.site-header__menu li > a.megamenu__card` to actually take effect (see the note there) — until then a card inside a column was 16px bigger on every side than the same card in a column cell, and the pill is drawn against the smaller one.
- **The dropdown is the one case where a top-level `<li>` is a positioning context**, and it needs three ancestors stretched to full header height (`.site-header__nav`, `.site-header__nav-scroll`, then the `<li>`) or `top: 100%` lands halfway up the header. The nav already asked for `height: 100%` and never got it: the header has no height of its own (the logo and the Call Us button set it), so the percentage resolved to auto — and `align-self: stretch` is ignored while a cross size is anything but `auto`, so the stretch has to come with `height: auto`.
- **The megamenu panel is anchored to `.site-header`, never to the `<li>`.** It spans the viewport minus 8px per side, so a `position: relative` on the parent `<li>` collapses it to the width of the nav item — that's why only nested parents (`.megamenu__item`, `.sub-menu` items) get a positioning context. It opens on `:hover`/`:focus-within`, no JS. The blurred backdrop over the page is `.megamenu::before`: `position: absolute` with `height: 100vh` so it starts below the header and can't cover it, and it sits on the outer `.megamenu` rather than `.megamenu__grid` because the grid clips its own rounded corners and would clip the backdrop with them.
- **The dropdown draws the same backdrop from a different element**, `.site-header::after` gated on `:has(.menu-item--dropdown:hover / :focus-within)`. The panel's own `::before` can't do it: the megamenu's version reaches the viewport edges with `left/right: -8px` only because that panel *is* the viewport minus 8px a side, where a 544px panel anchored to its nav item would need an inset big enough to reach both edges from wherever that item sits — not a number CSS can derive, and one that overflows the document if you guess it high. The header is already full-width and already the megamenu panel's positioning context, so `top: 100%` there lands on the same edge. Two elements, one look; `.megamenu::before` is switched off for dropdowns so they don't stack.
- **`logos-slider.php` renders all homepage logos** (certifications + clients) from the `client_logo` CPT as one Swiper marquee. There used to be a separate fixed-ACF row for certifications (`logos-bar.php`); that was removed and merged into this CPT-driven slider so the client can edit both from wp-admin without touching code.
- **Accordions are native `<details>`/`<summary>` with a shared `name`**, not JS. `faq.php` and `cards-stack.php` both rely on the browser's single-open enforcement (Baseline since late 2024); `src/base.css` only resets the marker and swaps the +/- icon off `[open]`. Don't reach for a JS accordion here. `cards-stack.php` nests this twice: `overview-accordion` picks the Start/Run/Grow row, and inside each row `overview-services-N` picks which of the three service cards is expanded (double width, image cropped to 412px, description + benefits revealed below; the other two stay full-height and image-only). All three services carry the same ACF fields — there is no "primary" card. A service with no description and no benefits renders no content block, and every "expanded" CSS rule is gated on `:has(> .overview-service-content)`, so such a card is inert rather than opening into an empty column; the card that starts expanded is the row's first service that has copy — all three rows currently open service 1. The column geometry (746px tall = 412px image + 8px gap + 326px min-height glass block) was measured off the Figma PNG exports at 1:1; an earlier 694px in this file came from the older design node and was wrong. The card's arrow-circle link lives inside its `<summary>`, so clicking it both navigates and toggles; that's invisible in practice, but a service left on the placeholder `#` link will expand and jump to the top instead of navigating.
- **Positional styling in the two "card" sections.** In `cards-deck.php` (Three Ways), `testimonials.php` (Google Reviews) and `hero.php` (photo fan) the card palette and tilt belong to the *slot*, not the content: a small JS file stamps `data-depth` / `data-pos` on each card and `src/base.css` keys the colours and rotation off that attribute. So the navy highlight always sits in the middle/front as the carousel moves, and the client can reorder or replace content without inheriting a colour.
- **Site-wide chrome (footer tagline, social links, WhatsApp number, header phone) uses `get_theme_mod()`**, not ACF-on-Home fields — those aren't Home-page content, they're global and would leak into every future page if scoped to one page's field group. Their Customizer controls live in `thinksme_customize_register()` under the "Site Details" section; if you add another `get_theme_mod()` call, register a control for it there too or the client can never change it.
- **Design-fidelity fallbacks.** Sections whose content comes from CPTs/menus render the Figma content as a fallback when nothing is set yet (`testimonials.php`, `faq.php`, `hero.php`'s photos, the header nav, the three `footer.php` link columns). This keeps the site looking like the design pre-content-entry; the fallback is plain text, never a link to a page that doesn't exist.

## Key constraint

The client must be able to edit content (text, images, list items) but must never be able to break the layout or structure. This drives the CPT/ACF split above — anything the client edits is either an ACF field on a fixed layout or a CPT post with a fixed template, never raw structural control.

## Commands

- `npm install` — install Tailwind
- `npm run dev` — Tailwind CLI watch build, `src/input.css` → `assets/css/style.css`
- `npm run build` — Tailwind CLI production build (minified)

Tailwind config lives at `src/tailwind.config.js` (not the repo root) — `content` globs scan `./**/*.php` from the repo root, so npm scripts must run from the theme root. Design tokens (colors, font sizes, radii, spacing) are defined twice by necessity: as CSS custom properties in `src/base.css` (imported at the top of `src/input.css`, ahead of the `@tailwind` directives) and mirrored in `theme.extend` in the Tailwind config so utility classes (`bg-brand-yellow`, `text-text-secondary`, `gap-lg`, etc.) match. When a token changes, update both files — the spacing scale in particular uses t-shirt-size keys (`xs`..`4xl`) that shadow Tailwind's default numeric spacing scale, so a typo there fails silently as an unstyled utility rather than a build error.

No PHP CLI is on `$PATH` in this environment — Local by WP Engine bundles its own, found at a path like `~/Library/Application Support/Local/lightning-services/php-<version>/bin/darwin-arm64/bin/php`. Use that binary for `php -l` syntax checks.

## Verification approach (per spec)

- Run the site in Local and compare the rendered homepage against Figma screenshots, section by section.
- Test content editing the way the client would (add a Google review, add a FAQ, add a client logo, change a Three Ways card) and confirm layout doesn't break.
- Check responsive behavior at mobile/tablet/desktop breakpoints.
- **Resizing the window through Chrome automation fires no `resize` and no `matchMedia` change events** — `innerWidth` updates silently. So anything keyed on a breakpoint *crossing* (`ci-plans.js`'s build/teardown) can't be exercised that way: load the page at each width instead, which enters the same states. Verified by installing probes for both events and watching them stay empty across four crossings.

## Deploying to production (InfinityFree) — Claude workflow

Live site: https://think-sme.infinityfree.me. Managed via two web tools (no saved credentials — if a session is expired, ask the user to log in manually in their own browser tab rather than guessing credentials):

- File Manager: https://filemanager.ai/new3/index.php (root = `/htdocs`). Deploy file changes by zipping the git delta (`git status --porcelain=v1 -uall`, everything modified/new since the last deploy) and uploading via "Upload & Unzip" (extracts in place, overwrites existing files). Always exclude this `CLAUDE.md` and `wp-content/mu-plugins/local-by-flywheel-live-link-helper.php` (Local-only dev file) from the zip.
- phpMyAdmin: https://php-myadmin.net/db_structure.php?db=if042648166_wp244 (server `sql107.infinityfree.com`, db `if042648166_wp244`).

**Database step — always get a FRESH export, never reuse an existing `app/sql/local.sql` file on disk.** That file is a point-in-time dump the developer exports manually and it silently goes stale — e.g. it can predate a page created or a menu link fixed directly in local wp-admin, which then quietly fails to make it into the live import even though the file sync looks complete. Confirmed root cause of a real bug (About Us page + its nav link existed in local wp-admin but not in the `local.sql` on disk).

Instead, pull the export live from Local by Flywheel's own DB admin UI, AdminNeo, which runs on the user's Mac at `http://localhost:10005/?username=root&db=local` (port may vary per site — ask the user for the current URL if it doesn't load, same as they gave it once before). Since this is on their machine, reach it with the `mcp__claude-in-chrome__*` browser tools (it loads like any other localhost page through their Chrome), not `device_bash`:

1. Navigate to the AdminNeo URL, click "Exportar" in the left sidebar.
2. Formato: SQL · Tablas: DROP+CREATE · Datos: INSERT · Salida: **guardar** (downloads a real file — "abrir" only renders ~50KB in-tab and truncates a multi-MB dump, useless here).
3. Click "Exportar". The file lands in the user's `~/Downloads` as `local-<timestamp>.sql`. If Downloads isn't already an accessible folder, call `device_request_folder_access` for it once, then `device_list_dir` to find the newest `local-*.sql`, then `device_stage_files` to pull it into the session.
4. Rewrite the domain (`http://thinksme-local.local` → `https://think-sme.infinityfree.me`, including the JSON-escaped form) recursively through nested PHP-serialized strings, recomputing `s:<len>:"..."` lengths after any content change — reuse/recreate the `rewrite.py` approach from past rounds rather than a naive find-replace, or serialized ACF/widget data will corrupt on unserialize.
5. Validate: count `s:<len>:"` strings whose declared length doesn't match their actual content, before and after rewrite. The counts should match exactly (there's a known pre-existing baseline, currently 21, unrelated to the rewrite) — a higher post-rewrite count means the rewrite broke something and must not be imported.
6. **Backup first, every time**: export the live DB from phpMyAdmin's Export tab (check "Agregar sentencia DROP TABLE / VIEW / PROCEDURE / FUNCTION / EVENT / TRIGGER" so it's a full-restore file) before importing anything, and get explicit confirmation from the user that the backup file downloaded before proceeding to Import.
7. Import the gzipped, rewritten SQL via phpMyAdmin's Import tab.
8. Verify on the live site: load a couple of pages touched by the change. If new content used a brand-new page template (`Template Name: ...` in a new `page-*.php`), check that an actual WordPress Page was created locally with that template assigned and that any nav/menu link meant to point to it isn't still a placeholder `#` — a template file alone renders nothing without a real Page behind it, and this has been the cause of "it's not showing up" more than once (same failure mode as orphaned media files uploaded to `wp-content/uploads` with no `client_logo` CPT post/attachment ever created to reference them).

